import { Address } from "./address";

export class User {
    customerId:number;
    firstName: string;
    lastName: string;
    email: string;
    mobileNumber: string;
    userId: number;
    address: Address;
}
